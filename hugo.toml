baseURL = 'https://ot.rohith.net/'
languageCode = 'en-us'
title = '#off-topic'

[params]
  defaultAvatar = "/images/default-avatar.png"

[markup.goldmark.renderer]
  unsafe = false  # Keep false for security with user-submitted content
